// Print All Elements of an Array

function printArray(arr, n) {
  for (let i = 0; i < n; i++) {
    console.log(arr[i]);
  }
}

const arr = ["Kia", "Citeron", "Hyundai", "BMW", "Audi", "Mahindra"];
const n = arr.length;

// printArray(arr,n);


// CRUD
